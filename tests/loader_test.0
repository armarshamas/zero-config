use ..src.schema.loader
use ..src.schema.types

pub fun test_loader() {
  // We can't do full I/O tests easily, but we can verify the API compiles.
  assert(true) // Minimal stub since I/O testing requires mocking
}